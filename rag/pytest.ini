[pytest]
testpaths = tests
markers =
    integration: needs a built index and the embedding model; skipped otherwise
# Integration tests are opt-in: the default run stays model-free so it can
# execute anywhere, including CI without a GPU or a downloaded index.
addopts = -m "not integration"
